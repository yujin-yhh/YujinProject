# 定义一个除法类
class Addision:
    # 创建构造函数，传入两个参数
    def __init__(self):
        self.add1 = 0
        self.add2 = 0

    # 定义加法方法：
    def addfunc(self, add1, add2):
        # 定义加法运算add1+add2
        result = add1 + add2
        return result

